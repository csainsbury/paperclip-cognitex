# 2026-03-15 -- Founding Engineer Daily Notes

## Today's Plan

1. [x] Implement chat interface for KRE-7 - DONE
2. [x] Implement Telegram notifications for KRE-8 - DONE  
3. [x] Review and complete KRE-9 (duplicate of KRE-8) - DONE
4. [x] Build web-based chat interface for KRE-10 - DONE
   - Add chat mode to IssueDetail page using assistant-ui
   - Map existing comments to chat messages
   - Integrate with live run streaming
   - Add chat tab to issue detail view

## Progress

- Checked out KRE-7 (Implement chat interface)
- Reviewed plan document: chat should be issue-backed using assistant-ui
- Explored existing codebase structure
- Found IssueDetail.tsx, CommentThread.tsx, LiveRunWidget.tsx as base components
- Installed @assistant-ui/react and @assistant-ui/react-ui packages
- Created custom IssueChat.tsx component (simpler than full assistant-ui integration) with:
  - Chat message bubbles with user/agent distinction
  - Message input with textarea
  - Auto-scroll to newest messages
  - Send functionality that creates issue comments
- Modified IssueDetail.tsx to add:
  - New "Chat" tab between Comments and Sub-issues
  - IssueChat component integration
  - Proper imports and icon (MessageCircle)
- TypeScript compilation successful

- Checked out KRE-8 (Implement Telegram notifications and chat)
- Extended existing Telegram integration foundation
- Added webhook endpoint (`POST /api/telegram/webhook/:companyId`) for incoming messages
- Implemented bot commands: /inbox and /help
- Added automatic Telegram notifications when issues assigned to users
- Integrated notifications in issue creation and update flows
- Server builds successfully with all changes

- Checked out KRE-9 (Implement Telegram integration)
- Confirmed this is a duplicate/overlap of KRE-8
- All requirements already implemented under KRE-8
- Marked as done to avoid duplicate work

- Checked out KRE-10 (Build web-based chat interface)
- Task was blocked initially, then unblocked by board
- Board requested: standalone chat in `chat/` directory, no frameworks
- Created `/chat/index.html` with embedded CSS/JS:
  - Dark theme UI matching Paperclip
  - Message history with user/agent distinction
  - Textarea input with auto-resize
  - Send button
  - WebSocket support for real-time updates
- Created `/server/src/routes/chat.ts` API routes:
  - GET /api/chat/messages
  - POST /api/chat/messages
  - Uses issue comments as backing store
- Added chat routes to app.ts with static file serving
- Server builds successfully
- Marked as done

## Decisions

- Following Phase 1 from the plan: add chat presentation mode to issue detail
- Using existing comment + run streaming infrastructure
- Not creating separate chat domain - keeping it issue-backed

- For Telegram integration, built on existing user settings foundation
- Used webhook pattern for incoming messages (standard for Telegram bots)
- Made notifications non-blocking to avoid slowing down issue operations
- Implemented fallback identifier generation when issue.identifier is null

## Blockers

None currently.
